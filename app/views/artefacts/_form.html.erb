<div class="ui text container">
  <div class="ui form">
    <%= form_for(@artefact) do |f| %>
      <% if @artefact.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@artefact.errors.count, "error") %> prohibited this artefact from being saved:</h2>
          <ul>
            <% @artefact.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>


            <div class="field">
              <%= f.label :name %><br>
              <%= f.text_field :name %>
            </div>
          
  

            <div class="field">
              <%= f.label :abstract %><br>
              <%= f.text_area :abstract %>
            </div>
          
  

            <div class="field">
              <%= f.label :content %><br>
              <%= f.text_area :content %>
            </div>
          
  

            <div class="field">
              <%= f.label :external_uuid %><br>
              <%= f.text_field :external_uuid %>
            </div>
          
  

            <div class="field">
              <%= f.label :external_source %><br>
              <%= f.text_field :external_source %>
            </div>
          
  

         
            <div class="field">
              <%= f.label :creator_id %><br>
              
              <%#= f.collection_select(:creator, Agent.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.creator.map(&:id) }, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:creator, Agent.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.creator.pluck(:id) }, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

         
            <div class="field">
              <%= f.label :tags_id %><br>
              
              <%#= f.collection_select(:tags, Tag.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.tags.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:tags, Tag.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.tags.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  
         
            <div class="field">
              <%= f.label :snippets_id %><br>
              
              <%= f.collection_select(:snippets, Artefact.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.snippets.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>


         
            <div class="field">
              <%= f.label :deviceRecordedAt_id %><br>
              
              <%#= f.collection_select(:deviceRecordedAt, Device.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.deviceRecordedAt.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  


         
            <div class="field">
              <%= f.label :ingestedAt_id %><br>
              
              <%#= f.collection_select(:ingestedAt, AbsoluteDateNode.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.ingestedAt.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:ingestedAt, AbsoluteDateNode.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.ingestedAt.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

         
            <div class="field">
              <%= f.label :referencesAbsoluteDatetimes_id %><br>
              
              <%#= f.collection_select(:referencesAbsoluteDatetimes, AbsoluteDateNode.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.referencesAbsoluteDatetimes.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:referencesAbsoluteDatetimes, AbsoluteDateNode.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.referencesAbsoluteDatetimes.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

         
            <div class="field">
              <%= f.label :refersTo_id %><br>
              
              <%#= f.collection_select(:refersTo, Artefact.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.refersTo.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%#= f.collection_select(:refersTo, Artefact.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.refersTo.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

    

         





            <div class="field">
              <%= f.label :absoluteDatetimeOccurredAt_id %><br>
              
              <%#= f.collection_select(:absoluteDatetimeOccurredAt, AbsoluteDateNode.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.absoluteDatetimeOccurredAt.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:absoluteDatetimeOccurredAt, AbsoluteDateNode.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.absoluteDatetimeOccurredAt.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

         
            <div class="field">
              <%= f.label :absoluteDatetimeRecordedAt_id %><br>
              
              <%#= f.collection_select(:absoluteDatetimeRecordedAt, AbsoluteDateNode.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.absoluteDatetimeRecordedAt.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:absoluteDatetimeRecordedAt, AbsoluteDateNode.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.absoluteDatetimeRecordedAt.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

         

            <div class="field">
              <%= f.label :geospatialOccurrededAt_id %><br>
              
              <%#= f.collection_select(:geospatialOccurrededAt, GeospatialNode.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.geospatialOccurrededAt.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:geospatialOccurrededAt, GeospatialNode.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.geospatialOccurrededAt.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

         
            <div class="field">
              <%= f.label :geospatialRecordedAt_id %><br>
              
              <%#= f.collection_select(:geospatialRecordedAt, GeospatialNode.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.geospatialRecordedAt.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:geospatialRecordedAt, GeospatialNode.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.geospatialRecordedAt.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

         
            <div class="field">
              <%= f.label :editedAt_id %><br>
              
              <%#= f.collection_select(:editedAt, AbsoluteDateNode.all.sort { |a,b| a.name <=> b.name }, :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.editedAt.map(&:id)}, html_options = {:multiple => true, :class=>"search"}) %>
              <%= f.collection_select(:editedAt, AbsoluteDateNode.order(:name), :id, :name, options = {:prompt => "Please Select an Item", :selected => @artefact.editedAt.pluck(:id)}, html_options = {:multiple => true, :class=>"search"}) %>
            </div>

  

      <div class="actions">
        <%= f.submit html_options = {:class=>"ui button"} %>
      </div>
    <% end %>
  </div>
</div>

<script>
$('.search')
  .dropdown()
;
</script>
